package com.ll.commands

/**
 * @Author liuliang
 * @Date   2019/4/10 0010 11:57
 */
object hql {
  //"drop table if exists links"
  //"create table if not exists links(movieId int,imdId int,tmdId int) stored as parquet"
  //"load data inpath '/tmp/links' overwrite into table links"
}
